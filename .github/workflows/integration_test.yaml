name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@integration-module-matrix
    secrets: inherit
    with:
      modules: '["test_charm", "test_relation"]'